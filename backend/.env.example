# Copy to `.env` and fill in.
# Generate a token with:  python -c "import secrets; print(secrets.token_urlsafe(18))"
# Must be at least 24 chars. Never commit the real .env.
IX_ACCESS_TOKEN=replace-with-24-char-random-token
